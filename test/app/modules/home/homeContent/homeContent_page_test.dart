import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('HomeContentPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(HomeContentPage(title: 'HomeContent')));
    //  final titleFinder = find.text('HomeContent');
    //  expect(titleFinder, findsOneWidget);
  });
}
