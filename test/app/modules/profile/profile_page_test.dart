import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:annaluxstore/app/modules/profile/profile_page.dart';

void main() {
  testWidgets('ProfilePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ProfilePage(title: 'Profile')));
    //  final titleFinder = find.text('Profile');
    //  expect(titleFinder, findsOneWidget);
  });
}
