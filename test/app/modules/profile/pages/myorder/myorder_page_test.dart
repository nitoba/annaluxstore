import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:annaluxstore/app/modules/profile/pages/myorder/myorder_page.dart';

void main() {
  testWidgets('MyorderPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MyorderPage(title: 'Myorder')));
    //  final titleFinder = find.text('Myorder');
    //  expect(titleFinder, findsOneWidget);
  });
}
