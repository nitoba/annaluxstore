import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('BuyPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(BuyPage(title: 'Buy')));
    //  final titleFinder = find.text('Buy');
    //  expect(titleFinder, findsOneWidget);
  });
}
