import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:annaluxstore/app/modules/checkout/checkout_controller.dart';
import 'package:annaluxstore/app/modules/checkout/checkout_module.dart';

void main() {
  initModule(CheckoutModule());
  // CheckoutController checkout;
  //
  setUp(() {
    //     checkout = CheckoutModule.to.get<CheckoutController>();
  });

  group('CheckoutController Test', () {
    //   test("First Test", () {
    //     expect(checkout, isInstanceOf<CheckoutController>());
    //   });

    //   test("Set Value", () {
    //     expect(checkout.value, equals(0));
    //     checkout.increment();
    //     expect(checkout.value, equals(1));
    //   });
  });
}
