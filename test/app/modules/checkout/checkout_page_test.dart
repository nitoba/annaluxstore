import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:annaluxstore/app/modules/checkout/checkout_page.dart';

void main() {
  testWidgets('CheckoutPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CheckoutPage(title: 'Checkout')));
    //  final titleFinder = find.text('Checkout');
    //  expect(titleFinder, findsOneWidget);
  });
}
